# Example environment variables for NoorHackGPT (Render / local)

# Database (Render will provide DATABASE_URL for Postgres)
DATABASE_URL=

# Optional: enable email verification sending (Gmail app password recommended)
EMAIL_USER=
EMAIL_PASS=

# Optional: reduce rate-limits on api.airforce (recommended)
AIRFORCE_API_KEY=

# Optional: override endpoint / throttle
AIRFORCE_API_URL=https://api.airforce/v1/chat/completions
AIRFORCE_MIN_REQUEST_INTERVAL_S=1.1

